{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "debug",
            "type": "lldb",
            "request": "launch",
            "program": "${cwd}/cub3d",
            "args": ["-c","maps/map1.cub"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Make build"
        }
    ]
}